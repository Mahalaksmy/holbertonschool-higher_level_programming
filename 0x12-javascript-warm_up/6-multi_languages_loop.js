#!/usr/bin/node
/* This a a script that prints 3 lines but using an array */
const lang = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; lang[i]; i++) {
  console.log(lang[i]);
}
